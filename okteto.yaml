build:
  server:
    context: server
  client:
    context: client

dependencies:
  todolist:
  repository: https://github.com/okteto/todolist-gitops-manifests
  wait: true

dev:
  server:
    command: bash
    sync:
      - server:/app
  client:
    command: bash
    sync:
      - client:/app